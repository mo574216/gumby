
experiment_name = "TriblerIdleRun"
workspace_dir = "./"
local_setup_cmd = "tribler_experiment_setup.sh"

build_swift = True

#PYTHONOPTIMIZE="yup"

local_instance_cmd = 'process_guard.py -t 120 -i 5 -c "tribler/tribler.sh "'

use_local_venv = False

tracker_cmd = ""

#Custom variable to control the experiment execution time in seconds
TRIBLER_EXECUTION_TIME = 30 # run for 60 seconds
